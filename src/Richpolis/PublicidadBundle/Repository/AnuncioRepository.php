<?php

namespace Richpolis\PublicidadBundle\Repository;

use Doctrine\ORM\EntityRepository;

/**
 * AnuncioRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class AnuncioRepository extends EntityRepository
{
    public function getMaxPosicion(){
        $em=$this->getEntityManager();
       
        $query=$em->createQuery('
            SELECT MAX(e.position) as value 
            FROM PublicidadBundle:Anuncio e 
            ORDER BY e.position ASC
        ');
        
        $max=$query->getResult();
        return $max[0]['value'];
    }
    public function getAnunciosActivos(){
        $em=$this->getEntityManager();
        $query=$em->createQuery('
               SELECT e 
               FROM PublicidadBundle:Anuncio e 
               WHERE e.isActive = :active
               ORDER BY e.position ASC
        ')->setParameters(array('active'=>true));
        return $query->getResult();
    }
}
